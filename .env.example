# =============================
# twic - Ejemplo de variables de entorno (MVP)
# Copiar como .env y ajustar según entorno de despliegue.
# =============================

# Identidad build (inyectado por pipeline normalmente)
GIT_SHA=
BUILD_DATE=

# Idioma por defecto y rutas de artefactos
DEFAULT_LANG=es
MODELS_DIR=models
DATA_DIR=data

# Backend de embeddings: placeholder (rápido, determinista) o st (sentence-transformers)
EMBEDDINGS_BACKEND=placeholder
EMBEDDINGS_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Pesos de fusión para clasificación híbrida (semántico / BM25 / clasificador)
ALPHA_SEM=0.5
BETA_BM25=0.3
GAMMA_CLF=0.2

# Umbral de abstención y top-k clasificación
TAU_LOW=0.4
TOP_K=20

# Pesos heurísticos búsqueda taxonomía
TAXO_W_EXACT=100
TAXO_W_PREFIX=60
TAXO_W_SUBSTRING=40
TAXO_W_ALT=30
TAXO_W_HIDDEN=20
TAXO_W_PATH=10
TAXO_W_CONTEXT=5
# Activar similitud vectorial asignando >0
TAXO_W_VEC=0
# Fuzzy: peso adicional y ratio mínimo
TAXO_W_FUZZY=0
TAXO_FUZZY_MIN_RATIO=70
TAXO_TOP_K=25

# Límites y observabilidad
ENABLE_METRICS=1
REQUEST_RATE_LIMIT=100
RATE_LIMIT_WINDOW_S=60
MAX_QUERY_CHARS=512
FASTAPI_ENABLE_DOCS=1

# Rate limiting distribuido (opcional). Ej: redis://host:6379/0
REDIS_URL=

# Toggle futuro (no requerido para MVP): API key / Auth -> pendiente
# API_KEY=

# Fin del archivo
